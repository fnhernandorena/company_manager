export interface StorageName{
    id: string;
    name: string;
}

 export interface RecievedName{
    id: string;
    name: string;
    updatedAt: string;
    createdAt: string;
}

export interface createName{
    name: string;
}