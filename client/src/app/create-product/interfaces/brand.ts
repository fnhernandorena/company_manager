export interface StorageBrand{
    id: string;
    name: string;
}

 export interface RecievedBrand{
    id: string;
    name: string;
    updatedAt: string;
    createdAt: string;
}

export interface createBrand{
    name: string;
}